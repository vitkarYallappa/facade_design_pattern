class UserValidation {
    validateUser(userId) {
      console.log(`Validating user with ID: ${userId}`);
      return true; // Assume the user is always valid
    }
  }
  
  module.exports = UserValidation;
  