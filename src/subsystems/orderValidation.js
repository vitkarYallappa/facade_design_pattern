class OrderValidation {
    validateOrder(orderId) {
      console.log(`Validating order with ID: ${orderId}`);
      return true; // Assume the order is always valid
    }
  }
  
  module.exports = OrderValidation;
  